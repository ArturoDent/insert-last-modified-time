import * as vscode from 'vscode';
import { EXTENSION_NAME, LMTSettings, getSettings } from "./configs";

export async function checkTemplate() {
  // must have %% LMT %% in it
  // can this be done in package.json?
}


// export async function checkOptions() {}